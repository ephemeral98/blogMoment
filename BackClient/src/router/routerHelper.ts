/* import Loadable from 'react-loadable';

//通用的过场组件
const loadings = () => 'loading...';

export default (loader, loading = loadings) => {
  return Loadable({
    loader,
    loading,
  });
};
 */
export default {};
